import React from 'react';
import {WebView} from 'react-native-webview';

const HiddenWebView = () => {
  return <WebView source={{uri: 'https://www.bighaat.com/'}} />;
};

export default HiddenWebView;
